/* See esp_lcd_touch.h: named in board.h, unused on this target. */
#pragma once

typedef struct esp_lcd_panel_t *esp_lcd_panel_handle_t;
typedef struct esp_lcd_panel_io_t *esp_lcd_panel_io_handle_t;
